% !TEX root = ./test-graphics.tex

\tikzset{
    contract/.style n args = {4}{
        draw,
        minimum width=1cm,
        minimum height=1cm,
        append after command={
            \pgfextra{
                \draw[red]
                    (\tikzlastnode.#1) -- +(#1:#2 cm);
                \draw[green]
                    (\tikzlastnode.#3) -- +(#3:#4 cm);    
            }
        }
    }
};

\node[contract={west}{30}{east}{60}] at (0, 0) {};